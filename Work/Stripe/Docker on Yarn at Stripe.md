#dockeronyarn #docker #yarn 

- How to support GPU with Docker on Yarn?
	- NVIDIA GPU drivers installed on each node.
	- NVIDIA Docker installed to enable GPU support.
- To access GPU, we need privileged docker containers. what needs to happen?
- How to properly cache images for all node managers?
- How to configure the network settings of Docker containers?
- How to centrialze the user and group management with docker containers?
- Docker daemon attack surface
	- Docker daemon requires root privileges. 