/*

# But track the .claude directory and its contents
!/.claude/
!/.claude/**

# Also track the .gitignore itself
!/.gitignore

# macOS
.DS_Store

# Obsidian workspace (if it somehow gets in .claude)
.obsidian/

# Python (if you have scripts in .claude)
*.pyc
__pycache__/
