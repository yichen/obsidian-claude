## 14:30 — LLM Infrastructure Deep Dive for Finance App

**What was done**:
- Deep Q&A on Cursor AI's business model (wholesale API pooling, rate limiting via proxy, local models on own infra)
- Explored self-hosting options for open-source models
- Discussed embedding local models in Electron apps (LM Studio, node-llama-cpp, Ollama sidecar)
- Evaluated Qwen3.5 models (newly released Mar 2, 2026) for potential use in finance app
- Discussed multi-model routing / intent classification patterns
- Compared OpenRouter vs Together AI pricing

**What was learned**:
- Cursor proxies all requests through their gateway for rate limiting/tracking; adds ~5-20ms latency but negligible vs LLM inference time
- Cursor's "Privacy Mode" = contractual zero-retention + no training data; enterprise gets BAA. Finance app's privacy pitch is stronger: DB stays local, LLM only sees query results not raw transactions
- LM Studio is an Electron app with llama.cpp bundled, provides OpenAI-compatible API at localhost:1234. Not an embeddable SDK — it's a UX wrapper for end users.
- node-llama-cpp is the actual npm package for embedding llama.cpp natively in Electron
- Qwen3.5 small series released March 2, 2026: 0.8B, 2B, 4B, 9B. Apache 2.0. The 9B reportedly beats GPT-4o-mini and can run on 16GB Mac.
- 7B Q4 model = 4.1GB file, needs ~6GB RAM. Unworkable on 8GB MacBook Air M1 (OS takes 3-4GB already).
- Together AI: open-source models from $0.10/M tokens. Llama 3.1 8B, Qwen models available. Cheaper than OpenRouter for OSS (OpenRouter adds ~5% fee).
- Claude 3.7 Sonnet on OpenRouter: $3/M input, $15/M output
- The finance app's localIntentRouter (line 367, finance-core.ts) already does keyword-based routing — upgrading to ML-based routing (RouteLLM, Semantic Router) would be next level but current keyword approach already works well
- 0.5B-0.8B models: only viable for classification/routing, not SQL generation with multi-table JOINs
- Multi-model routing saves 60-70% on API costs by routing 70%+ of simple queries to cheaper models

**What was decided**:
- Embedded 7B model in app is NOT the right path (too heavy for low-spec users, huge download)
- Optional Ollama sidecar is the pragmatic middle ground for local/offline mode
- Worth testing Qwen3.5-9B for SQL generation to potentially replace Sonnet on simple queries

**Open threads**:
- Test Qwen3.5-9B SQL generation quality vs Claude Sonnet on the finance app's actual query patterns
- Implement multi-model routing: Together AI for simple queries, Sonnet for complex ones
- Evaluate Together AI as direct integration (skip OpenRouter for OSS models)

**Files touched**:
- None (conversation only)

**Retain**:
- Qwen3.5 small series released March 2, 2026 (0.8B–9B); 9B beats GPT-4o-mini, runs on 16GB Mac, Apache 2.0 — prime candidate to replace Claude Sonnet for simple finance queries at $0.10/M vs $15/M.
- Finance app privacy pitch: SQLite DB stays local on user's machine; LLM only sees query results, not raw transactions — stronger privacy guarantee than Cursor's privacy mode.
- Together AI for open-source models is ~5% cheaper than OpenRouter for same models; for Claude you must use OpenRouter/Anthropic directly.
- Embedding a 7B model in the Electron app requires 4.1GB download + 6GB RAM — not viable for general consumers; use optional Ollama sidecar pattern instead.
- Multi-model routing pattern: route ~70% of simple lookups to cheap 8B model (~$0.10/M), reserve Sonnet ($3/$15/M) for complex multi-step SQL/reasoning queries.

---

## 20:59 — Demo App Chart Fix, Latency Analysis, Slash Command Registration

**What was done**:
- Fixed broken chart rendering in demo-app: replaced `file://` URL with base64 data URL (`data:image/png;base64,...`) — Electron CSP blocks `file://` in renderer
- Analyzed 21s latency: 98% is 3 sequential OpenRouter API calls (3.5s + 7.7s + 9.4s). SQL/tools only ~400ms total
- Removed `node_modules/` from git history using `git filter-repo` — 159MB Electron binary was blocking GitHub push
- Created 15 symlinks from `.claude/commands/` → `.claude/skills/` to make skills appear in `/` autocomplete
- Committed and pushed changes to `origin/main`

**What was learned**:
- Electron's Content Security Policy blocks `file://` URLs in the renderer process. Fix: read PNG as Buffer in main process, convert to base64, send as data URL
- Claude Code has two separate slash command registries: `.claude/commands/*.md` for `/` autocomplete; `.claude/skills/<name>/SKILL.md` for AI-invocable Skill tool. Need symlinks to bridge.
- `git rm -r --cached` only removes from current tracking, NOT from history. Must use `git filter-repo` to rewrite history for large blobs.
- `git filter-repo` removes the remote after rewriting — must re-add and force push
- OpenRouter API latency for Claude Sonnet: 3.5–9.4s per call. Number of API round trips is the dominant factor, not tool execution time.

**What was decided**:
- Use base64 data URLs for all chart rendering in Electron (not file:// or custom protocols)
- Symlink approach for commands→skills to avoid content duplication
- Latency optimization: reduce API round trips by removing "always run dashboard" instruction

**Open threads**:
- Demo-app latency optimization not yet implemented (reduce from 3 to 2 API round trips)
- Pre-fetching SQLite data into memory cache for common queries (discussed, not implemented)
- Multi-model routing exploration continues (Qwen3.5-9B, Together AI)

**Files touched**:
- `/Users/yichen/Obsidian/Projects/2_personal-finance-llm/demo-app/electron/finance-core.ts` — base64 chart conversion, chartData in ChatResult
- `/Users/yichen/Obsidian/Projects/2_personal-finance-llm/demo-app/src/components/MessageBubble.tsx` — chartData base64 instead of file://
- `/Users/yichen/Obsidian/Projects/2_personal-finance-llm/demo-app/src/lib/types.ts` — added chartData field
- `/Users/yichen/Obsidian/Projects/2_personal-finance-llm/demo-app/electron/preload.ts` — pass chartData through IPC
- `/Users/yichen/Obsidian/.claude/commands/` — 15 new symlinks to skills

**Retain**:
- Electron blocks `file://` URLs in renderer via CSP — always use base64 data URLs for local images in Electron apps
- Claude Code `.claude/commands/` and `.claude/skills/` are separate registries; symlinks from commands→skills bridge both autocomplete and AI invocation
- `git filter-repo --path <dir> --invert-paths --force` removes a directory from all git history; remote must be re-added after
- Demo-app latency is 98% LLM API calls — reducing round trips (not SQL speed) is the only meaningful optimization

---

## 22:45 — Dashboard Pie Charts, Chat Whitespace, Pending Transactions Review

**What was done**:
- Added `income_by_source` chart type to `generateChart()` in `finance-core.ts` — pie chart grouping payslips by employer
- Added `income_by_source` to the `generate_standard_chart` tool enum
- Extended Dashboard overview to load 4 charts in parallel (income bar + pie, spending bar + pie)
- Rendered pie charts below bar charts in each overview card with divider + subtitle labels ("By Source", "By Category")
- Removed "Spending Dashboard" suggestion chip from ChatInterface (now always visible on Dashboard)
- Removed unused `PieChart` import from ChatInterface
- Reduced chat whitespace: messages-area gap 16→8px, padding 24→16px 20px, message-row gap 8→4px
- Changed suggestions grid from 4 to 3 columns (matching 3 remaining chips)
- Confirmed Fix 2 (pending transactions) was already implemented — `migratePendingYamlToSQLite()` already called on startup at line 41 of `index.ts`

**What was learned**:
- `migratePendingYamlToSQLite()` was already wired up in `electron/index.ts` — the plan assumed it wasn't being called, but it was

**What was decided**:
- Skipped Fix 2 implementation since it was already done
- 3-column grid for suggestions after removing "Spending Dashboard" chip

**Open threads**:
- Need to rebuild (`npm run dev` / `npx electron-vite build`) and verify all 3 fixes visually
- Multi-model routing still pending as next major feature

**Files touched**:
- `/Users/yichen/Obsidian/Projects/2_personal-finance-llm/demo-app/electron/finance-core.ts` — added `income_by_source` chart type + tool enum entry
- `/Users/yichen/Obsidian/Projects/2_personal-finance-llm/demo-app/src/components/Dashboard.tsx` — 4-chart parallel load, pie chart rendering below bar charts
- `/Users/yichen/Obsidian/Projects/2_personal-finance-llm/demo-app/src/components/ChatInterface.tsx` — removed "Spending Dashboard" chip + unused PieChart import
- `/Users/yichen/Obsidian/Projects/2_personal-finance-llm/demo-app/src/styles.css` — added divider/subtitle-row classes, reduced chat gaps, 3-col suggestions grid

**Retain**:
- Dashboard now shows 4 overview charts: income bar + pie (by employer), spending bar + pie (by category) — all loaded in parallel via `Promise.all`
- `income_by_source` chart type queries `SELECT employer, SUM(net_pay) FROM payslips GROUP BY employer` — works because user has two employers (Salesforce, ServiceTitan)
- Pending YAML import (`migratePendingYamlToSQLite`) was already wired up on app startup — plan's assumption that it was missing was incorrect
- Chat whitespace fix: messages-area gap 16→8px, padding 24→16px, message-row gap 8→4px significantly tightens bubble spacing

---

## 21:28 — Archive Schedule Extraction & 1040 Computation Engine Investigation

**What was done**:
- Implemented full archive PDF form extraction: `map_archive_pages()` detects 19 IRS forms/schedules by header signatures
- Built Phase 1 parsers: Schedule D, Form 8949, Schedules A/1/2/3 — each extracts line items to individual YAMLs
- Shared `_extract_schedule_amount()` helper with continuation-line support and anti-false-positive regex
- Updated `process_archive_pdf()` to orchestrate all parsers after 1040 extraction
- Added 3 new cross-validation checks (Schedule D→1040 L7, Schedule A→deductions, Schedule 1→L8/L10) — all MATCH across 2020-2024
- Updated carryforwards to log Schedule D lines 6/14/16 when available
- Fixed pre-existing 1040 deductions line prefix bug ("12a" → "12")
- Investigated 1040 computation engine accuracy (11.1%) and traced root causes
- Deep-dived into 2024 capital gains data to understand where CPA adjustments come from

**What was learned**:
- IRS form line numbers in PDF text (e.g., "through 16. Also") match dollar amount regex `\d+\.\d{0,2}` — must reject amounts followed by alphabetic text on the same line
- IRS forms can have 5+ continuation lines between label and value (Schedule D lines 1a/8a) — need wide search window but must stop at next form line label
- Adjacent dollar amounts on IRS forms (e.g., "141,030. 152,518. -11,488." for proceeds/cost/gain) get filtered by proximity check — only check proximity against parenthesized amounts
- `carryforwards.yaml` was fundamentally wrong: used raw 1099-B totals instead of Schedule D data. 2022 raw 1099-B showed $217K gain but Schedule D had -$70,707 loss. $67,707 should have carried forward to 2023 but didn't.
- The $220K capital gains gap (TAX-001) breaks down: $67K from missing carryforward + $152K from non-covered RSU/ESPP adjusted cost basis
- Non-covered RSU basis adjustments come from stock plan administrator (Morgan Stanley), not from Fidelity supplemental pages
- Fidelity "Supplemental Information" (pages 7-9 of consolidated 1099 PDFs) only contains dividend/interest detail — NOT capital gains cost basis adjustments
- 2024 capital gains gap is only $1,559 vs 2023's $220K — nearly all 2024 transactions were "covered" (Fidelity tracked basis correctly)
- The CPA reclassified $42K of 2024 short-term transactions and adjusted cost basis ($162K raw → $128K adjusted), but the net effect was minimal ($1,559)

**What was decided**:
- Use 4 parallel agents for archive parser implementation (page mapper, Schedule D/8949, Schedules 1/2/3/A, cross-validation) — all editing different functions in same 5K-line file
- Capital gains accuracy is fundamentally limited by non-covered RSU/ESPP cost basis data for older years (2020-2023)
- Plan to improve compute_1040.py with fixable gaps: carryforward fix, W-2 dedup, backdoor Roth detection, Schedule 2 taxes, deduction fix

**Open threads**:
- 1040 computation engine improvements not yet implemented (plan written at `/Users/yichen/.claude/plans/sorted-singing-sunrise.md`)
- User considering whether to find stock plan administrator data (Morgan Stanley) for RSU vesting records
- Phase 2/3 archive parsers (Schedule C/E/H, Form 8606/8889/2441, etc.) not yet implemented
- TAX-001 and TAX-002 status should be updated in tax-issues.yaml

**Files touched**:
- `/Users/yichen/Obsidian/.claude/scripts/ingest_tax.py` — Added ~800 lines: `map_archive_pages()`, `_extract_schedule_amount()`, `_find_amounts_on_line()`, `parse_schedule_d_archive()`, `parse_form_8949_archive()`, `parse_schedule_a_archive()`, `parse_schedule_1_archive()`, `parse_schedule_2_archive()`, `parse_schedule_3_archive()`. Updated `process_archive_pdf()`, `run_cross_validate()`, `run_carryforwards()`. Fixed 1040 deductions prefix.
- `/Users/yichen/Obsidian/Finance/tax/archive/2020-2024/` — New YAMLs: schedule-d.yaml, form-8949.yaml, schedule-a.yaml, schedule-1.yaml, schedule-2.yaml, schedule-3.yaml for each year

**Retain**:
- Archive PDF form extraction produces individual YAMLs per form: `Finance/tax/archive/<year>/schedule-d.yaml` etc. Schedule D line 16 matches 1040 line 7 perfectly across all 5 years (2020-2024).
- The $220K capital gains discrepancy (TAX-001) is $67K from broken carryforward computation + $152K from non-covered RSU/ESPP adjusted cost basis that only the CPA has (from stock plan administrator, not Fidelity supplemental).
- For 2024, raw 1099-B ($127,616) nearly matches Schedule D ($126,057) — only $1,559 gap. Most non-covered RSU shares were sold by 2023, so future years should have small gaps.
- `compute_1040.py` at `.claude/scripts/compute_1040.py` is the 1040 computation engine (733 lines) — currently 11.1% accuracy, with a plan to improve to 60%+ by fixing carryforwards, W-2 dedup, backdoor Roth, Schedule 2 taxes, and deduction logic.
- Fidelity supplemental pages (pages 7-9 of consolidated 1099 PDFs) contain only dividend/interest breakdowns, NOT the adjusted cost basis for RSU/ESPP capital gains.

---
