## 08:37 — Dashboard Upcoming Section Implementation

**What was done**:
- Implemented "Upcoming" section in Dashboard.tsx showing predicted financial events for next 30 days
- Two data sources: paycheck prediction from `payslips` table, recurring expense detection from `fidelity_cma_transactions`
- Recurring CMA detection uses SQL pattern: categories with 3+ occurrences in last 6 months, avg interval 7-45 days
- Added `UpcomingEvent` interface, `formatRelativeDate()` helper, state + useEffect for data loading
- Added `.upcoming-*` CSS styles — compact list with 1px gap separators matching existing dashboard aesthetic
- Section placed between Overview Charts and Today's Insights
- TypeScript compiles cleanly

**What was learned**:
- The `fidelity_cma_transactions` table has categorized descriptions that naturally group recurring payments (child support, spousal maintenance, mortgage, HELOC, CC payments)
- Pattern detection (3+ occurrences, consistent cadence) is more robust than hardcoding transaction types

**What was decided**:
- Used intelligent discovery approach over hardcoded categories — detects any recurring pattern automatically
- Confidence: 'high' for 5+ occurrences, 'medium' for 3-4
- Green for income, neutral text color for expenses (not red)

**Open threads**:
- Verify actual predicted dates against real transaction history once app is running
- May want to add click-to-deep-dive on upcoming events later

**Files touched**:
- `/Users/yichen/Obsidian/Projects/2_personal-finance-llm/demo-app/src/components/Dashboard.tsx` — added UpcomingEvent interface, formatRelativeDate(), upcomingEvents state, useEffect with two SQL queries, JSX section
- `/Users/yichen/Obsidian/Projects/2_personal-finance-llm/demo-app/src/styles.css` — added .upcoming-* styles

**Retain**:
- Dashboard now has forward-looking "Upcoming" section that predicts paychecks and recurring CMA payments using historical pattern detection
- Recurring payment detection query: categories in fidelity_cma_transactions with 3+ negative-amount occurrences in 6 months and 7-45 day average interval
- Paycheck prediction: last pay date + average interval per employer from payslips table, with while-loop to advance past today
- Demo app lives at `/Users/yichen/Obsidian/Projects/2_personal-finance-llm/demo-app/`

---
